# stock-signal 索羅門任務檔 — 2026-08-02 第二輪（分析驗證機制 + 語言習慣詞典 + 報告介面3項）

> 讀完本檔前，先讀通用章程 `D:\All claude\000_Agent\006_institution\pm_agent_solomon.md`。
> 這輪任務跟前一輪（`stock-signal_TASK_2026-08-03.md`，介面卡片化+關注度排序）完全獨立，已完工上線，不用理會。

> **2026-08-02 02:42 修訂（取代下方原本的監督模式/截止時間/1b範圍/明確排除清單，其餘章節不變）**：
> 使用者在主控 session 對話中把這輪任務從「今晚22:00前、有人監督」擴大成「現在起算8小時、無人監督」，同時追加3項報告介面優化、放寬1b可以自己判斷要不要接進`prompt.py`、並且**這次特別把「絕對紅線」的範圍縮小成只剩「會花錢」一條**（push/對外發送/密鑰/正式DB破壞性寫入/刪使用者原始資料這五類，這次全部改成跟其餘分岔點一樣走自主判斷+標記+事後報告，不再單獨停手）。完整理由與範圍見下方新增的「6. 本輪特別授權」一節——**這是使用者當面反覆確認兩次、且在被主控session明確指出跟`CLAUDE.md`既有NEVER清單衝突後仍維持的裁決，只適用這一次任務檔，不是通用章程的永久修改**。
> 讀到這份任務檔的索羅門，請先讀完整份文件（含新增第6/7節）再開工，不要只看到「無人監督」就假設沿用章程預設的五類絕對紅線。

## 0. 這輪任務怎麼來的

使用者反映：怎麼知道 Gemini 分析股癌逐字稿的正確性？主控 session 已經用 EP680 做過一次真人試跑，證實 Gemini 單模型確實會犯錯——EP680「台積電」被 Gemini 判 `-1`（看空，High信心），但主控 session 另外呼叫 DeepSeek 分析同一集，DeepSeek 判 `+1`（看多），兩邊分歧。送 Codex(`gpt-5.6-terra`，read-only 仲裁)逐字稿全文覆核，Codex 明確判定 DeepSeek 對：Gemini 把「股價短期跌6%後主持人轉防守模式」誤當成對台積電基本面看空，但主持人整集對台積電法說會與估值的敘述全是正面（capex上修/毛利優於財測上緣/營收展望上修/明確說「台積電根本不應該在2300這個價格」）。**這筆已由主控session直接修正DB**（`signals.id=954`，`action` -1→+1，屬已放寬的自主範圍），這輪不用再處理這一筆。

使用者接著提出更深一層的問題：分歧仲裁不能只靠通用產業鏈規則，需要真正熟悉股癌本人的語言習慣（暗語、黑話、不直接點名標的的隱性指涉、反諷/自嘲風格）才抓得出更細緻的誤判。討論後有兩條路：①打造「股癌語言習慣詞典」注入 prompt 當額外背景知識（成本低、可以快速驗證）②真的 fine-tune 一個專屬模型（成本高、要先驗證①的天花板在哪）。使用者裁決：**先做①，②之後有機會再說，這輪不用碰**。

## 1. 任務清單

### 1a. Gemini/DeepSeek 分歧仲裁工具（新模組）

**目的**：讓「這集分析到底準不準」變得可查證，不再是黑盒信任單一免費模型。

- 新增 `crosscheck.py`，核心函式建議 `run_crosscheck(episode_id: str) -> dict`：
  1. 讀 `transcripts/` 對應集數逐字稿全文
  2. 呼叫 DeepSeek（沿用主控 session 這輪試跑驗證過的呼叫方式：`requests.post("https://api.deepseek.com/chat/completions", ...)`，`model="deepseek-chat"`，`system` 訊息用 `prompt.py::SYSTEM_PROMPT`，金鑰讀 `os.environ["DEEPSEEK_API_KEY"]`，**不落地明碼、不印出金鑰本身**）拿到這集的 `extracted_signals`
  3. 跟 DB 裡這集 Gemini 分析出的既有 `signals` 資料比對，用 `stock_code` 對齊，判定「分歧」的條件（兩者皆需觸發，缺一都算分歧，AI暫定可調整）：
     - 同一 `stock_code` 兩邊 `action` 不同
     - 一邊有抓到某 `stock_code`、另一邊完全沒有這筆（不對稱偵測，這輪試跑時 Tesla/飛捷 6206.TW 就是這種情況）
  4. 有分歧時，組出跟主控 session 這輪試跑同款的仲裁 prompt（產業鏈判定規則 + 兩模型判斷 + 逐字稿全文），呼叫 `codex exec -s read-only -c model_reasoning_effort=high`（**不要**加 `model_provider` 覆蓋，用 config.toml 目前的預設模型 `gpt-5.6-terra`，這是使用者這輪明確指定的），拿到仲裁結論與理由
  5. **只記錄不自動改 `signals` 表**：結果寫進新表 `signal_review`（純附加 DDL，`CREATE TABLE IF NOT EXISTS`，屬已放寬自主範圍，dry-run 後可直接執行）欄位建議：`id SERIAL PRIMARY KEY, episode_id TEXT, stock_code TEXT, gemini_action TEXT, deepseek_action TEXT, codex_verdict TEXT, codex_reasoning TEXT, reviewed_at TIMESTAMP DEFAULT now()`。**絕對不要**在這個函式裡對 `signals` 表做任何 UPDATE——分歧仲裁結果只供人審閱，要不要據此修正正式訊號是主控 session/使用者的決定，不是索羅門自動執行的範圍（EP680 那筆是使用者在場時主控 session 手動做的個案，不是常態自動化）。

- **這次先跑一個小批次驗證工具本身有效，不要backfill全部680集**（範圍/成本是主控session保留的另一個決定，這輪只需要證明工具能用）：對 EP677、EP678、EP679 三集各跑一次 `run_crosscheck()`（EP680 已經人工驗證過，可以拿來當「這個工具重跑一次應該要跑出同樣分歧」的迴歸測試，若重跑結果跟主控session手動試跑的不一致要停下來查原因，不要假設是正常誤差）。
- **不接進 GitHub Actions / 週排程**——本機執行即可，理由：Codex CLI 用的是本機 ChatGPT 登入 session（`~/.codex/auth.json`），不是可攜的 API key，貿然接進 CI 需要把個人登入憑證匯出成 GitHub Secret，這件事主控 session 已經明確判斷過不擅自做，這輪也不用碰。
- 成本追蹤：DeepSeek 每集約 US$0.0025（16K input+700餘output tokens量級，主控session已用EP680實測過），Codex 仲裁只在真的有分歧時才觸發，用同一套 `grep balance` 方法記錄花費，計入這次任務的 Codex 50點標準預算。

### 1b. 股癌語言習慣詞典（Phase 1，研究型任務）

**目的**：先用低成本方式驗證「餵給模型股癌的個人語言習慣背景知識」有沒有用，不直接跳去 fine-tune。

- 讀 `transcripts/` 目錄全部逐字稿（680 集，`episodes.json` 有完整清單可核對數量），整理出股癌反覆使用的：
  - 不直接點名標的、但固定用某種說法指涉特定產業/個股的隱性指涉（例如某些綽號、某類「業內都懂」的簡稱）
  - 個人慣用的反諷/自嘲/誇飾句型（跟 `prompt.py` 現有 Rule 1「反串過濾機制」互相對照，看有沒有現有規則沒覆蓋到的變體）
  - 反覆出現的比喻或口頭禪，會影響「看多/看空/中立」判讀的（例如某種語氣詞代表他其實在講反話）
- **技術做法交給索羅門判斷**（680 集全部塞進單一 context 大概率超過視窗，需要自己設計批次處理/彙整方式——例如分批讀 N 集歸納一次，再把多批結果做二次彙整去重），用哪個模型跑這個研究任務也自行判斷（Gemini 免費額度或 DeepSeek 都可以，注意別撞到既有 pipeline 的 Gemini rate limit，發現撞到就換一個模型或放慢，不要硬重試）。
- **產出**：一份參考文件（建議 `docs/host_idiom_glossary.md` 或类似位置，索羅門自行決定合理路徑），條列整理出的模式，每條附至少1-2個逐字稿原文範例佐證，不要只有抽象描述沒有例證。
- **（2026-08-02 02:42 修訂，取代原本「不要接prompt.py」的限制）**：詞典產出後，**這次授權你自己判斷要不要接進 `prompt.py` 的 `SYSTEM_PROMPT`、怎麼接（整份塞進去 vs 挑重點）**，不用等使用者先看過內容審閱。但這會直接影響之後每一集正式 Gemini 分析的判讀品質，屬於高留痕要求的自主決策：
  1. 動手前先備份/記錄修改前的 `SYSTEM_PROMPT` 完整內容（commit 本身就是最好的備份，改之前先確認乾淨 commit）。
  2. 接進去的版本要挑幾集（建議含至少1-2集`transcripts/`裡實際出現過該類黑話/隱性指涉的舊集數）重新跑一次分析（可以用 dry-run 或不寫入DB的方式測，避免真的觸發付費API還要另外考慮花錢紅線），比對接詞典前後的判讀差異，證明有正面效果或至少沒有讓判讀變差，不能只憑「感覺應該有幫助」就直接上線。
  3. 完工報告要清楚交代：接了多少內容、怎麼接的（整份/摘要/挑重點）、比對驗證的證據、殘餘風險（例如詞典條目本身如果有誤判可能反而誤導未來分析）。
  4. 這個決定影響往後所有正式排程分析，屬於本任務檔「6. 本輪特別授權」放寬範圍內的一般分岔點，不用問Codex，但完工報告要標成醒目段落，讓使用者第一眼就看到「這輪動了SYSTEM_PROMPT」。

### 1c. 個股排行預設篩選範圍：全部 → 最新20集

- 使用者反映全集勝率統計樣本數太混雜，代表性不夠，預設應該窄一點
- `report_html.py` 第789行 `let _sr = 0, ...` 改成 `let _sr = 20, ...`
- 同檔第412-415行 button 區塊，`btn-active` class 要從 `id="sr-0"`（全部）移到 `id="sr-20"`（最新20集），兩處要同步改，不要只改其中一個造成初始畫面跟資料範圍對不上
- Playwright 驗證：開啟報告頁確認初始渲染就是最新20集資料、「最新20集」按鈕確實是高亮狀態，且切換到其他範圍按鈕功能不受影響

### 1d. 逐字稿詳細頁（新增，2026-08-02 02:42 補，目標：純瀏覽方便）

**目標（使用者已確認）**：讓使用者能在報告網站上直接看逐字稿全文，單純方便閱讀，不是設計成訊號查核工具（不用刻意對應到某筆訊號跳轉）。

**現況**：`transcripts/` 目錄有680份`.md`逐字稿，目前完全沒有任何網頁介面能瀏覽，只能開本機檔案。`episodes.json`有集數↔標題↔日期的對照，可以拿來組列表。

**技術方案（主控session依「產品思考流程可自主決策」條款拍板，非使用者逐項核准，完工後主控session會在報告中看到並可提出調整）**：
- 新增 `report_html.py::generate_html_transcripts()`，輸出獨立頁面（例如`report_transcripts.html`，部署後對應`transcripts.html`，比照`report_attention.html`的部署模式加進兩個GitHub Pages workflow）
- 頁面結構：集數列表（依`episodes.json`日期新到舊排序，比照既有`_ep_num()`/日期排序邏輯），每集一個可展開/收合區塊，展開後渲染該集`.md`全文（純文字即可，不需要markdown語法渲染，逐字稿本身多半是純對話文字）
- 680份逐字稿全部塞進單一HTML的JSON blob可能導致頁面過大（可先估算.md檔案總大小，超過某個量級如5MB就要考慮拆頁或lazy load，索羅門自行判斷合理閾值並在報告中說明依據）
- 效能與安全：逐字稿文字一律套用既有`escapeHtml()`/`_esc()`，加簡單全文搜尋（比照1c的搜尋框模式），不需要進階功能（不用做分頁跳轉到訊號、不用做逐句時間軸）
- 入口連結：仿照`report_attention.html`目前的做法在`report_detail.html`header加一個入口連結（後續會被1e的分頁籤設計取代/整合，不用重複做兩次UI，1e完成後1d的入口連結直接改成分頁籤的一部分）

**完成的定義（本項專屬）**：
1. 680集全部可瀏覽或有清楚交代的取捨（例如lazy load門檻），沒有集數遺漏或讀取失敗未提示
2. 全文搜尋功能可用，Playwright驗證搜尋關鍵字能正確定位到集數
3. 頁面在合理時間內載入完成（貼出實測載入時間），若因檔案量體大而需要分批/lazy load，要說明機制與驗證證據

### 1e. Nav 切換開關：三頁像分頁籤並列切換（新增，2026-08-02 02:42 補，目標：使用者已確認）

**目標（使用者已確認）**：報告頁（`index.html`/`report_detail.html`）、關注度頁（`attention.html`）、逐字稿頁（`transcripts.html`，1d新增）三者要像分頁籤一樣並列呈現，使用者一眼就知道三個頁面都存在、可以互相切換，不是像現在這樣只有一行不顯眼的小連結。

**現況**：目前只有`report_detail.html`header裡一行`<a href="attention.html">`小連結（見`report_html.py`第299-303行），`attention.html`/`transcripts.html`（1d新增後）沒有任何回連或平行連結。

**技術方案（主控session拍板）**：
- 三個頁面是各自獨立生成的靜態HTML（無SPA路由、無共用JS bundle），「分頁籤」做法用**視覺上像tab、實際是三個獨立超連結**的方式實作（例如header下方一列`<a>`用tab樣式呈現，目前所在頁面對應的tab加`active`樣式），不用引入額外框架
- 三個頁面（`report_html.py`裡三個`generate_html_*()`函式）的header都要組裝同一組tab列，用一個共用函式（例如`_render_nav_tabs(active: str)`）產生對應HTML，避免三處各寫一份導致風格漂移
- 拿掉1d/現有attention連結那種「小字+透明背景」樣式，改成三個並列、有明確active狀態的tab按鈕
- Email版（`generate_html_email()`）**不用加**這個tab列，Email是獨立情境，不受這項影響（比照1e既有排除慣例）

**完成的定義（本項專屬）**：
1. 三個頁面header都能看到同一組tab、目前頁面正確標示active狀態，Playwright三頁各截圖驗證
2. 從任一頁點另外兩個tab都能正確跳轉，連結路徑在部署後（`_site/`結構）沒有404

### 1f. Onboarding：全面新手導覽（新增，2026-08-02 02:42 補，目標：使用者已確認）

**目標（使用者已確認）**：不只針對關注度頁，而是整份報告（卡片、sparkline、chip顏色慣例、勝率定義等）都要有新手導覽，讓第一次看到這份報告的人能看懂在看什麼。

**現況**：目前`report_detail.html`已有「勝率定義說明」等零星的文字說明穿插在各區塊，沒有集中的導覽機制；`report_attention.html`只有首屏一句警語「反映節目近期討論熱度，不是買賣建議」。

**技術方案（主控session拍板）**：
- 每個頁面（報告/關注度/逐字稿）header下方新增一個可收合的「怎麼看這份報告」說明區塊，內容依頁面性質各自客製（例如報告頁講卡片chip顏色/sparkline/持倉天數的意思，關注度頁講分數怎麼算+既有警語，逐字稿頁講純瀏覽用途）
- 用`localStorage`記使用者是否已關閉過導覽（key例如`sig_onboard_dismissed`），**只在使用者從未關閉過時預設展開**，關閉過就記住不再自動展開，但保留一個小小的「？」按鈕可以隨時再打開——不要每次都硬塞在使用者眼前
- 純前端localStorage判斷，不需要後端/DB配合，三個頁面各自獨立判斷（不用共用同一個key跨頁記憶，保持實作單純）
- 文案要求：講人話、簡短，不要變成落落長的使用手冊，每頁不超過5條重點

**完成的定義（本項專屬）**：
1. 三個頁面都有導覽區塊，首次造訪（清localStorage）截圖驗證預設展開、內容正確
2. 關閉後重新整理頁面，確認導覽保持收合狀態（localStorage生效），「？」按鈕能重新叫出來
3. 文案經索羅門自行撰寫，完工報告附上實際文案內容讓使用者能直接審閱、不滿意可以之後再改

## 2. 明確排除（這輪不做，避免自作主張擴大範圍）

- fine-tune 專屬模型——使用者已裁決這輪不碰，之後才可能考慮
- 對 `signals` 表的任何自動修正——1a的分歧仲裁只記錄不動手改既有訊號
- 1d/1e/1f 以外，任何進一步的報告介面重新設計（例如再做新的視覺方案比較demo）——這輪範圍就是1d/1e/1f三項定義的內容，不要因為做順手了就繼續擴大改別的區塊

## 3. 範圍限定

只能動 `D:\All claude\300_Projects\stock-signal\` 目錄底下的檔案；例外只有依通用章程要追加的記憶檔。不得跑 `codex` 以外的付費 API 呼叫方式去動 Gemini 正式分析 pipeline（`analyzer.py`/`batch.py`/`update.py` 這輪不用改動，1a的DeepSeek呼叫是新模組自己獨立呼叫，不影響既有分析流程）。

## 4. 監督模式與截止時間（2026-08-02 02:42 修訂，取代原本22:00/有人監督）

- **監督模式：無人監督**——使用者不可及（睡眠中），命中章程「監督模式」一節的無人監督原則：真正卡住的子任務標記`blocked`寫進報告，不依賴它的其餘任務繼續做，不要整個停擺。
- **截止時間：2026-08-02 上午 10:42**（從任務檔這次修訂時間 02:42 起算8小時，使用者本人指定的窗口）。
- 建議施工順序：1c（5分鐘小改動先建立信心）→ 1a（分歧仲裁工具+小批次驗證）→ 1e（nav分頁籤，先把三頁的骨架/共用函式建好，後面1d/1f都要用到）→ 1d（逐字稿頁）→ 1f（onboarding，依賴前面三頁都存在才能各自客製文案）→ 1b（研究型任務，時間彈性最大，放最後，做到多少算多少）。
- **版本錨點 commit hash**：這次修訂請先 commit 本任務檔（哪怕只有文字修訂），`git log -1 --format=%H` 記下的 hash 即為這次擴大範圍後的版本錨點，覆蓋原本錨點。

## 5. 完成的定義

1. 1a：`crosscheck.py` 對 EP677/678/679 三集實際跑過，貼出每集是否偵測到分歧、分歧內容、Codex仲裁結論（如果那三集剛好都沒分歧也要老實說，不能為了「展示有東西」硬找分歧）；`signal_review` 表建表+寫入有實際驗證（查DB確認筆數）；明確驗證過**沒有**對 `signals` 表做任何寫入
2. 1b：產出的詞典文件有實際貼出至少5條整理出的模式範例（含逐字稿原文佐證），並老實交代處理了多少集（680集全部/取樣多少），取樣的話說明取樣邏輯；若判斷接進`prompt.py`，比照1b段落內的4點留痕要求逐條核對
3. 1c：Playwright 截圖或文字描述確認預設20集生效
4. 1d/1e/1f：各自完成的定義見對應小節，三項都要Playwright實測（不是只看程式碼邏輯正確）
5. Codex/DeepSeek 花費各自累計並回報（沿用grep balance方法），50點/US$5預算内——**這個預算上限本身不受第6節紅線放寬影響，一樣是硬上限**
6. 完工報告清楚標注每一項各自「真的完成」vs「待你審閱」的部分，尤其1a的分歧仲裁結果、1b的詞典內容（與是否接了prompt.py）都是要给使用者看過的重點，不是自動生效就沒事的功能

## 6. 本輪特別授權：絕對紅線縮小為僅剩「花錢」一條（2026-08-02 02:42 新增）

**這是使用者在主控 session 對話中明確、反覆確認兩次的裁決**（第一次確認後，主控session指出這跟`D:\All claude\CLAUDE.md`裡的NEVER清單衝突並列出具體歷史事故——2026-07-02正式站全掛、2026-07-05/06密鑰印進對話、2026-07-12正式DB直接修改+訂閱者token印進對話——使用者看過這些事故對照後仍維持「拔紅線」的決定）。**只適用這份任務檔這一次，不是通用章程的永久修改**，下一輪任務檔沒有另外重新裁決，一律恢復章程原本的五類絕對紅線。

**這次唯一保留的真正停手條件**：會產生實際金錢花費且**超出本任務檔第5節第5點的Codex 50點/DeepSeek US$5預算**——超出才停手；預算內的花錢行為（例如DeepSeek呼叫、Codex呼叫）本來就在既有授權範圍內，不用另外停。

**這次改成自主判斷+標記+事後報告的五類**（原章程「絕對紅線」一節列的其餘四條，這次全部降級成跟一般分岔點一樣處理）：
1. push（原本絕不push）
2. 對外發送任何東西（email/API/部署）
3. 碰密鑰/跑會dump環境變數的指令
4. 對正式DB的破壞性寫入（DELETE/DROP/TRUNCATE，或覆蓋既有資料值的UPDATE/ALTER）
5. 刪除使用者原始資料

**即使上述五類這次不強制停手，仍然要做的事（這不是「可以隨便做」，是「用判斷代替停手」）**：
- 動手前用一句話寫下「我要做X，因為Y，風險是Z，回復方式是W」（比照章程「建議項目自主執行機制」的四要素），寫進完工報告的 `autonomous_decisions` 或新增一個 `redline_relaxation_actions` 欄位專門記錄這五類底下實際發生的行為，不要跟一般分岔點混在一起，方便使用者事後優先審視
- 涉及第4類（正式DB破壞性寫入）：一律還是要先跑唯讀dry-run列出受影響範圍，只是不用停手等回覆，dry-run結果連同「我要執行了」的判斷一起寫進報告
- 涉及第1類（push）：push前列出這次要推的commit清單摘要，寫進報告，不要只寫「已push」三個字
- 如果對某個行為判斷不出風險高低（不是「有點猶豫」而是真的沒把握），**還是可以選擇保守：標記blocked、不動手、寫進報告**——這條授權是「拿掉強制停手」，不是「禁止你自己選擇不做」

**範圍不含**：本任務檔第3節「範圍限定」（只能動`stock-signal`目錄）依然有效，這次紅線放寬不等於可以跑到其他專案目錄。
