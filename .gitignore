# 環境與密鑰
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# 逐字稿（從 whatmkreallysaid.com 動態下載，不進 repo）
transcripts/

# 2026-08-02 索羅門新增（任務1d）：transcripts_data/ 是 export_transcripts_data()
# 從 transcripts/*.md 複製出的純文字版本（供逐字稿頁前端 fetch 用），純衍生產物、
# 可隨時重新產生，不需要進 repo（比照 transcripts/ 本身也不進 repo 的既有慣例）。
transcripts_data/

# 2026-08-15 索羅門：獨立轉錄的影音中間產物（yt-dlp 下載的影片/聲音、.srt/.mkv）。
# 原本放在 transcripts_data/independent_media/，但那個目錄整包會被 `cp -r` 進 _site
# 公開發佈——已搬到這裡（見 independent_transcribe.py 的 INDEPENDENT_MEDIA_ROOT 註解）。
# 這一行擋的是「進 public repo」，擋不到「進 Pages」——後者由 check_site_payload.py
# 的白名單負責。**兩個是不同的門，各擋各的，不要以為 gitignore 有寫就安全了。**
media_work/

# 資料庫（本機用，不進 repo）
*.db
*.db-wal
*.db-shm

# 產出物
report_preview.html
batch.log
*.log
episodes.json

# 本地開發工具
.vscode/
.idea/

# 資料庫備份（含訂閱者 email 等個資，stock-signal 是 public repo，絕不可進這裡）
backups/

# 所有 .env 變體（.env.neon、.env.bak_* 等，一律不進 public repo）
.env*

# canary 的本機 state（每次執行覆寫，不需要進版控）
canary_state.json
