# 提案：stock-signal 產品重心從「標多空」換成「名單本身」

- **性質**：調查結果 + 變更清單。**本輪一行執行碼都沒改、DB 零寫入、零寄信、零付費 API 呼叫。**
- **裁決依據**：丹尼爾 2026-08-24 已裁決換方向（`serenity-clone/SOLOMON_HANDOFF.md` 第 0 節末「這一條要丹尼爾裁決」）
- **寫成時間**：2026-08-24
- **寫的人**：subagent（唯讀調查），未派下層 subagent

---

## 三句話版本

1. 外部回測證明：**股癌講到哪幾檔是有價值的（+8.2pp，統計上站得住）；他說看多還看空量不出價值（+2.2pp，誤差範圍蓋過 0）。**
2. 好消息是「名單」這個產品**已經有半套了**——`attention.py` 算的關注度排行完全不看多空，而且已經有一個公開頁 `attention.html`，只是被放在第二個分頁。搬到主位就是最小的第一步。
3. 壞消息是資料層有個洞：**現有 973 筆訊號裡，284 筆「中性」訊號一筆進場價都沒有**（程式三處都寫死 `WHERE action != '0'` 把它們排除）。要拿名單當產品，這 284 筆得補價 —— 那是 DB 寫入，**要你點頭才做**。

---

# 第一部分：證據到底說了什麼

## 1-1 五個數字各自的定義（每個都附字串座標）

「字串座標」= 可以在該檔案裡 `grep` 到的唯一字串。本工作區已裁決行號是假座標。

| 數字 | 它到底是什麼 | 母體 N | 期間 | 出處（字串座標） |
|---|---|---:|---|---|
| **56.34%** | 股癌的**方向性立場**在持有 21 個交易日後**超額報酬為正**的比率（美股對 SPY、台股對 0050.TW，進場變體 next_open）。這是主終點。 | **662**（wins=373，242 集） | 2023-11-11 ~ 2026-08-05 | `serenity-clone/PROGRESS.md` 的 `觀察 56.34% (373/662)`；正本 `serenity-clone/data/stockpick_significance.json` 的 `"observed_win_rate": 0.5634441087613293` |
| **48.15% / 48.17%** | **選股基線**：保留該筆 `direction`，**換一檔同日同市場的股票**。回答的問題是「他挑的股票，比同一天隨機抓一檔好嗎」。實作在 `core/pipeline.py::direction_matched_baseline`。 | 同上 662 | 同上 | `PROGRESS.md` 的 `既有 \`direction_matched_baseline\` **沒有 bug**`；JSON 的 `"published_win_rate": 0.481526` 與 `"mean_win_rate": 0.48173972809667676` |
| **+8.2pp**（精確 **+8.17pp**） | 56.34% − 48.17%。**「他挑的股票好不好」的答案。** 差值 95% CI **[2.76, 13.63]pp**（配對重抽），**明確排除 0**。 | 662 | 同上 | `PROGRESS.md` 的 `\| all \| 56.34% (n=662, 242 集) \| 48.17% \| **+8.17pp**`；JSON 的 `"diff_point": 0.08170438066465258` |
| **54.15%** | **方向虛無**：保留股票不動，**打亂 `direction` 標籤**（保持 long/short 比例）10000 次。回答的問題是「他說看多看空這件事本身有價值嗎」。實作在 `tools/significance.py`。 | 662 | 同上 | `SOLOMON_HANDOFF.md` 的 `方向虛無 54.15%`；正本 `data/significance.json` 的 `"null_mean": 0.5415442598187311` |
| **+2.2pp**（精確 **+2.19pp**） | 56.34% − 54.15%。**「他的多空判斷有沒有價值」的答案。** 差值 95% CI **[-2.22, +6.31]pp**，**涵蓋 0**。 | 662 | 同上 | `data/significance.json` 的 `"diff_pp": 2.189984894259822`、`"ci_covers_zero": true` |
| **+0.4pp**（精確 **+0.42pp**） | 同上一格，但只算 `forward_only`（真的在預測未來的句子）。54.78% − 54.35%，CI [-5.08, +5.97]，**涵蓋 0**。 | **314**（wins=172） | 同上 | `data/significance.json` 的 `"diff_pp": 0.42305732484076986` |

### 為什麼 +8.2pp 和 +2.2pp 不矛盾（`SOLOMON_HANDOFF.md:113` 特別警告的那點）

**因為它們是在跟兩個不同的對照組比，回答兩個不同的問題。**

用一句人話講：8.2pp 是「他點名的這幾檔，比同一天隨機抓一檔好多少」；2.2pp 是「知道他看多還看空，比擲硬幣決定方向好多少」。前者是**選股**，後者是**方向**。

三段分解（`PROGRESS.md` 的 `## 三段分解（近似，非正式檢定）`）：

```
48.17%（隨機一檔 ＋ 他的方向）→ 54.15%（他的標的 ＋ 打亂方向）→ 56.34%（他的標的 ＋ 他的方向）
        └── 選股貢獻 ≈ +6pp ──┘        └── 方向貢獻 ≈ +2.2pp（CI 涵蓋 0）──┘
```

**價值在他提到了哪幾檔，不在他說看多還看空。**

---

## 1-2 🔴 p 值對帳：`p=0.0005` 查無來源

`000_Agent/001_memory/daily/2026-08-24.md` 那一行寫「+8.2pp, p=0.0005 vs +0.4pp」。**這個 p 值在 serenity-clone 全專案裡完全不存在。**

對帳指令與輸出：

```
$ cd "D:/All claude/300_Projects/serenity-clone"
$ grep -rn "0\.0005\|0\.0001\|0\.0056\|0\.0182\|0\.3963" --include="*.md" --include="*.json" --include="*.py" --include="*.html" .
  （0 筆 0.0005 的命中；命中的全是 0.0182 / 0.3963 / 0.0001 / 0.0056）
```

**真正存在的四個 p 值，各自屬於哪個比較：**

| 比較 | 差值 | permutation p | 出處 |
|---|---:|---:|---|
| **選股**（vs 選股基線）· all | +8.17pp | **0.0001**（精確 9.999e-05，單尾、加一修正） | `data/stockpick_significance.json` 的 `"p_value_one_sided": 9.999000099990002e-05` |
| **選股** · forward_only | +7.28pp | 0.0056 | `PROGRESS.md` 的 `\| forward_only \| 54.78% (n=314, 163 集) \| 47.49% \| +7.28pp` |
| **方向**（vs 方向虛無）· all | +2.19pp | 0.0182 | `data/significance.json` 的 `"permutation_p": 0.018198180181981802` |
| **方向** · forward_only | +0.42pp | 0.3963 | `data/significance.json` 的 `"permutation_p": 0.3962603739626037` |

**結論**：`p=0.0005` 是 daily log 抄寫時產生的錯值，來源不明。**要引用「+8.2pp 有統計顯著性」時應寫 `p=0.0001`（單尾 permutation），不是 0.0005。**
而且 daily log 那一行把「+8.2pp（選股·all）」跟「+0.4pp（方向·forward_only）」並排、中間掛一個 p——**這兩個數字連母體都不同**（662 vs 314），並排本身就會誤導。

## 1-3 引用這些數字時**必須同時講**的限制（漏講就是誇大）

1. **選股基線 48% 低於 50% 本身就有意義**：代表「他池子裡隨機一檔贏過 SPY/0050 的機率只有 48%」——這是中位數個股輸給市值加權指數的常見現象。所以「+8pp」的一部分是**「他挑到贏過平均個股的股票」，跟「他贏過指數」是兩件事**。（`PROGRESS.md` 的 `**選股虛無 ≈48%（低於 50%）本身就有意義**`）
2. **抽樣池只有他講過的 158 檔，不是全市場**。若選擇效應發生在標的層級（他只講強勢股），這兩把尺量不出來。
3. **`forward_only` 版的選股差值 CI 下界剛好是 0.00pp**（非四捨五入）——只有 all 那版明確排除 0。
4. **追漲**：18.6~23.2% 的立場出現在該標的已漲 >10% 之後（動量延續，不是事前預測）。
5. **標註品質**：抽 40 筆盲判，方向一致率只有 **65.0%**。**「方向量不出價值」有可能是方向欄位本身標得不準，不等於「他沒有方向判斷力」。** 這一條對本提案特別重要——見第三部分 L2 的風險欄。
6. **沒有對照組**（同期間、同產業、他沒提到的股票）。serenity-clone 自己把這列為下一棒第一優先（`SOLOMON_HANDOFF.md` 的 `0. 🔴 **建對照組**`）。
7. `p=0.0182` 與「差值 CI 涵蓋 0」看似打架：permutation 未做叢集、拔靴有做叢集，**以叢集版為準**，所以寫「看不出價值」而**不是「證明沒有價值」**。

### 兩個小落差（不是矛盾，但引用時要對齊）
- `48.15%` vs `48.17%`：前者是 `core/pipeline.py` 印的隨機基線（`published_win_rate: 0.481526`），後者是 `tools/stockpick_significance.py` 用 10000 次 permutation 重跑的平均（`0.48173`）。**兩者用不同 seed/rounds 重建同一個東西，差 0.02pp，不是 bug。** `SOLOMON_HANDOFF.md` 用 48.15 算出 +8.2pp，`PROGRESS.md` 的檢定表用 48.17 算出 +8.17pp，**指的是同一件事**。
- `SOLOMON_HANDOFF.md` 摘要表寫「+2.2pp，差值 CI [-2.2, +6.3] 涵蓋 0」與詳表的 `[-2.2224, +6.3107]` 是同一組數，只是位數。

---

# 第二部分：「標多空」在 stock-signal 裡的實際 footprint

## 2-1 母體對帳（附指令與輸出）

```
$ cd "D:/All claude/300_Projects/stock-signal"
$ git ls-files | wc -l
133                                    # 全 repo 追蹤中的檔案

$ git ls-files | grep -E '\.(py|md|html|yml)$' | tr '\n' '\0' | xargs -0 grep -lE '\baction\b' | wc -l
25                                     # 含 action 字樣的檔（含歷史審查稿與 argparse 誤命中）

$ git ls-files | grep -E '\.(py|md|html|yml)$' | grep -vE '^(_verify_2026-08-11|self_improvement)' \
    | tr '\n' '\0' | xargs -0 grep -cE '\baction\b' | grep -v ':0$' | sort -t: -k2 -rn
report_html.py:20   report_detail.html:10  crosscheck.py:10  attention.py:9
database.py:7       performance.py:6       report.py:5       prompt.py:5
line_query.py:5     welcome_email.py:3     notifier.py:3     migrate.py:3
update.py:2         stock-signal_TASK_2026-08-02_r2.md:2     sync_independent_transcripts.py:1
stock-signal_TASK_2026-08-03.md:1        split_sentinel.py:1  restore_db.py:1
migrate_to_neon.py:1                     canary.py:1          batch.py:1
```

⚠️ **這 21 檔不能直接當答案**：`welcome_email.py`、`notifier.py`、`canary.py`、`batch.py`、`update.py`、`crosscheck.py:345`、`sync_independent_transcripts.py` 的部分命中是 **argparse 的 `action="store_true"`，與多空無關**。逐筆看過後的真名單在 2-2。

中文方向詞的另一把尺：
```
$ git ls-files | grep -E '\.(py|md|html|yml)$' | grep -vE '^(_verify_2026-08-11|self_improvement)' \
    | tr '\n' '\0' | xargs -0 grep -clE '多空|看多|看空|看好|看壞' | wc -l
16
```

**資料層母體（來自本機備份快照，未連線 DB）：**
```
$ python -X utf8 -c "import json,collections; d=json.load(open('backups/backup_stocksignal_db_2026-08-08.json',encoding='utf-8')); \
  s=d['signals']; print(len(s)); print(dict(collections.Counter(x.get('action') for x in s))); \
  print(dict(collections.Counter((x.get('action'), bool(x.get('entry_price'))) for x in s)))"
973
{'0': 284, '+1': 638, '-1': 51}
{('0', False): 284, ('+1', True): 634, ('-1', True): 50, ('+1', False): 4, ('-1', False): 1}
```
→ **973 筆訊號、266 集、215 個不重複代號。方向性立場 689 筆（638 多 + 51 空），中性 284 筆（29.2%）。
🔴 這 284 筆中性訊號，`entry_price` 全部是空的——一筆都沒有。**
（684 筆 vs serenity-clone 的 `directional=683`：差在它做了同日去重，不是矛盾。）

## 2-2 逐項清單：哪個檔在做什麼 / 換方向後要變成什麼

| # | 檔案（字串座標） | 這裡在做什麼 | 換方向後要變成什麼 | 層 |
|---|---|---|---|---|
| 1 | `prompt.py` · `# Rules & Constraints` 底下 Rule 1/2/5/6 | **成本中心**。7 條規則有 4 條在教模型判多空：反串過濾（把 action 打成 0）、+1/-1/0 判準、產業鏈多空 CoT 推理基準、同集唯一淨訊號（禁止同集同標的同時 +1 與 -1） | Rule 5 整段移除或降為「僅供 reasoning 參考」；Rule 6 從「淨多空」改成「同集同標的只留一筆、保留最完整的 exact_quote」；Rule 1 保留（反串過濾仍需要，但改成過濾「這檔標的算不算真的被提到」而非「方向是不是 +1」） | L2 |
| 2 | `prompt.py` · `"action": "+1 或 -1 或 0"` | Output Schema 的必填欄位 | 改 optional（可為 `null`／`"未判定"`），欄位保留 | L2 |
| 3 | `prompt.py` · `def rule_version()` | SYSTEM_PROMPT 內容雜湊前 12 碼，寫進 signals 表 | **不改，但要知道：prompt 改一個字雜湊就變**，這正是用來切分新舊資料的工具 | L2 |
| 4 | `analyzer.py` | 只驗 JSON 結構（`extracted_signals` 必須是 list、元素必須是 dict），**完全沒碰 action** | **不用改**（好消息） | — |
| 5 | `database.py` · `action               TEXT,` | signals 表的欄位定義 | **欄位保留、不 drop**（歷史資料要能重算、要能回滾） | L2 |
| 6 | `database.py` · `if code in seen and seen[code] != action` | 同集衝突攔截：同一集同一代號出現互相矛盾的 action 就丟棄後來那筆 | action 變 optional 後這段會失效 → 改成**只用 `code` 去重**，保留 quote 最長的那筆 | L2 |
| 7 | `performance.py` · `WHERE action != '0' AND (entry_price IS NULL` | `_fill_entry_prices()`：**只給有方向的訊號補進場價** | 拿掉 `action != '0'` → 284 筆中性訊號才會有價。**這會產生 DB 寫入** | L2 🔴 |
| 8 | `performance.py` · `SELECT * FROM signals WHERE action != '0' ORDER BY entry_date ASC` | `calc_performance()`：**中性訊號整批不進績效** | 同上。名單當產品 = 名單上每一檔都要能追蹤 | L2 🔴 |
| 9 | `performance.py` · `elif action == "-1":` 附近的報酬正負號翻轉 | 看空時報酬反號才算贏 | 保留（有 action 的舊資料仍要能算） | L2 |
| 10 | `split_sentinel.py` · `WHERE action != '0' AND stock_code IS NOT NULL` | 分割哨兵：檢查近 21 天的股票分割，清掉受影響的價格快取 | **現況等於「中性訊號的標的分割了不會被偵測」**——名單化之後這是真的資料錯誤，必須拿掉 `action != '0'` | L2 🔴 |
| 11 | `report.py` · `def action_label(action: str, confidence: str)` | CLI 報告的「動作」欄：超級看好／看好／看壞／中性 | 降級成次要欄位；主欄改成「被提及次數 / 關注度」 | L1 |
| 12 | `report.py` · `for action_key, lbl in [("+1", "看好/超級看好"), ("-1", "看壞")]` | 分方向的勝率細分 | 保留但加註「方向本身量不出價值」 | L1 |
| 13 | `report_html.py` · `const actionFull = (a, c) =>` | 詳細報告頁 JS：把 +1/-1/0 轉成「超級看好／看好／看壞」 | 降級 | L1 |
| 14 | `report_html.py` · `const dirLabel = g.dir === '-1' ? '↓ 看空'` 與 `const dirLbl = s.action === '-1' ? '↓ 看空'` | 個股卡片與訊號列的方向 chip（主視覺） | 從主視覺降成小字灰標籤 | L1 |
| 15 | `report_html.py` · `def generate_html_email(` 裡的 `r.get("action") in ("+1", "-1")` | **信件的「🔥 本週最新訊號」明確排除中立訊號** | 改成「本週名單」＝最新 2 集提到的**全部**標的（含 action=0） | L1 🔴 對外 |
| 16 | `report_html.py` · `badge_txt = "超級看好"` / `= "看好"` / `= "看壞"` | 信件卡片右上角的彩色 badge，是整封信最搶眼的元素 | 降級為小字；主 badge 改成「本集第 N 次提及」或關注度分數 | L1 🔴 對外 |
| 17 | `report_html.py` · `<b style="color:#aaa;">勝率</b>：主委看好/看壞的標的` | 信件裡對「勝率」的定義說明 | 補一句誠實聲明：本勝率只涵蓋有方向的訊號；獨立回測顯示**方向本身量不出價值（+2.2pp，CI 涵蓋 0）**，量得出價值的是名單（+8.2pp，CI [2.8, 13.6]pp） | L1 🔴 對外 |
| 18 | `attention.py` · `def compute_attention(` | **關注度分數 A = Σ(信心權重 × 2^(-age/H))。完全不看 `action`。** | ✅ **不用改——這就是「名單」產品的引擎，已經在跑了** | — |
| 19 | `attention.py` · `bull_w = sum(w for w, i in weighted if i.get("action") == "+1")` 與 `def consensus_label(` | 方向共識（近期偏多／偏空／立場分歧），是 A 之外的**獨立**欄位 | 降級為輔助欄位，加註「僅供參考，回測量不出價值」 | L1 |
| 20 | `attention.py` · `key = (ep_num, code, action)` | 去重三元組含 action | action 變 optional 後要改成 `(ep_num, code)` | L2 |
| 21 | `report_html.py` · `_NAV_TABS = (` → `("report", "index.html", "📊 訊號報告")` 排第一、`("attention", "attention.html", "🔥 目前關注度")` 排第二 | 三頁導覽順序 | **關注度（名單）排到第一**；或改 workflow 的 `cp report_attention.html _site/index.html` | L1 |
| 22 | `report_html.py` · `"「近期立場分歧」＝加權後多空接近、講者立場不明確，不是無訊號"` | 關注度頁的新手導覽 bullets | 改寫，把「名單怎麼用」放主位 | L3 |
| 23 | `line_query.py` · `("+1", "High"):   "🔥 超級看好",` 與 `non_zero = [s for s in signals if s.get("action") != "0"]` | LINE 查詢的表情符號標籤，同樣排除中性 | 降級 + 拿掉排除 | L1 |
| 24 | `crosscheck.py` · `elif str(g.get("action")) != str(d.get("action")):` | **拿 DeepSeek 交叉驗證 Gemini 的 action 判定**——這是純為「標多空」而存在的付費流程 | **整支可以停用**（換方向後，該交叉驗證的是「標的有沒有抓漏」，不是「方向對不對」） | L2 |
| 25 | `notifier.py` · `subject = f"【股癌訊號追蹤】{title}  勝率 {stats['win_rate']}%` | **信件主旨把「勝率」當主角** | 主旨改成名單導向（例：本週 N 檔上榜、M 檔新進榜） | L3 🔴 對外 |
| 26 | `welcome_email.py` · `🔥 <b>本週最新訊號</b>——最新一集提到的標的、方向與節目原文引用` | 訂閱歡迎信的產品承諾 | 改寫成名單導向 | L3 🔴 對外 |
| 27 | `welcome_email.py` · `股癌訊號勝率追蹤` 與 `歡迎訂閱｜股癌訊號勝率追蹤週報` | 產品名 | 改名（例：股癌關注名單追蹤） | L3 🔴 對外 |
| 28 | `migrate.py` / `migrate_to_neon.py` / `restore_db.py` 的 DDL | schema 裡的 action 欄位 | **不改**（欄位保留） | — |
| 29 | 靜態產出 `report_detail.html`、`report_attention.html`、`email_preview.html`、`email_subscriber_preview.html` | 2026-08-11 產生的快照 | **不手改**，改完程式重新產生即可 | — |
| 30 | `stock-signal_TASK_2026-08-02_r2.md`、`_2026-08-03.md`、`_2026-08-11.md` | 歷史任務書 | **不改**（歷史紀錄），裁決紀錄留在本檔 | — |
| 31 | `check_site_payload.py` · `("attention.html", "關注度頁")` | 出門檢查白名單 | index.html 與 attention.html **都已在白名單**，L1 若只調順序**不用改白名單**；若新增頁面才要改 | L1 |

### 🔴 本輪最有價值的發現

**「名單」這個產品已經有半套了。** `attention.py::compute_attention()` 的關注度分數 `A` 只吃「提及次數 × 信心權重 × 時間衰減」，**完全不看方向**；方向只影響另一個獨立欄位 `consensus`。而且它已經有一個公開頁 `attention.html`，只是被排在導覽的第二個位置。

**換重心的 L1，主要工作是「把已經蓋好的那一頁搬到主位」，不是從零蓋。**

---

# 第三部分：三層變更清單

## L1 — 只改呈現，不動資料（風險最低，建議先做）

**改哪些檔**：`report_html.py`（`_NAV_TABS`、`generate_html_email` 的 `r.get("action") in ("+1", "-1")`、三個 `badge_txt` 分支、`勝率</b>：主委看好/看壞的標的` 那句、`dirLabel`/`dirLbl`/`actionFull`）、`report.py`（`action_label`）、`line_query.py`（`ACTION_LABEL`、`non_zero`）、`attention.py`（`consensus_label` 的措辭）

**改成什麼**：
1. 導覽第一頁改成「關注度／名單」。
2. 週報信件的「🔥 本週最新訊號」改成「🔥 本週名單」，收錄最新 2 集提到的**全部**標的（含 action=0 的 284 筆同類），方向從彩色 badge 降成小字灰標。
3. 績效區塊保留但加誠實註記：勝率只涵蓋有方向的訊號；獨立回測顯示方向本身量不出價值（+2.2pp，CI [-2.2, +6.3] 涵蓋 0），量得出價值的是名單（+8.2pp，CI [2.76, 13.63]，p=0.0001）。

**風險**：
- 🔴 **會改到訂閱者實際收到的信** → 對外發送、不可回復。**必須丹尼爾拍板。**
- 名單區塊要顯示 action=0 的標的，但**這些標的沒有 entry_price**（284 筆全空），所以名單卡片上**不能顯示報酬**，只能顯示「被提到」。若要顯示報酬就得先做 L2 的回填。這一條容易在實作時才發現，先寫在這裡。
- 導覽對調若改動檔名（`index.html` ↔ `attention.html`），會打斷既有信件裡 `detail_url` 指向的舊連結。**建議檔名不動、只調順序與首頁頂部區塊次序。**

**怎麼驗收**：
```
python -X utf8 notifier.py --no-fill --preview        # 只存 HTML 預覽，不寄信
grep -c "本週名單" email_preview.html                  # 應 ≥ 1
python -X utf8 check_site_payload.py _site            # 出門檢查仍須通過
```
再目視 `email_preview.html`：名單區塊在績效區塊之前、且至少含一筆 action=0 的標的。

**能不能回滾**：能。純呈現層，`git checkout -- report_html.py report.py line_query.py attention.py` 即可，DB 一個 byte 都沒動。**但已經寄出去的信收不回來**——這就是要拍板的原因。

---

## L2 — 改 Gemini 指令與資料欄位（中風險）

**改哪些檔**：`prompt.py`（Rule 1/2/5/6 + Output Schema 的 `action`）、`database.py`（`if code in seen and seen[code] != action` 的去重邏輯）、`performance.py`（兩處 `WHERE action != '0'`）、`split_sentinel.py`（一處 `WHERE action != '0'`）、`attention.py`（`key = (ep_num, code, action)`）、`crosscheck.py`（停用方向交叉驗證）

**改成什麼**：
- prompt.py：Rule 5（產業鏈多空 CoT 基準）整段拿掉或降為 reasoning 參考；Rule 6 從「淨多空」改成「同集同標的只留一筆」；Schema 的 `action` 改 optional。
- 三處 `WHERE action != '0'` 拿掉 → 中性訊號才會補價、進績效、被分割哨兵保護。
- 去重 key 從含 action 改成只用 code。

**對既有歷史資料的影響**（這一段是本層最重要的）：
- **欄位不 drop、既有 973 筆的 action 值不改**。歷史勝率仍可完整重算，可完全回滾。
- 新資料的 action 會變成 null／未判定 → **新舊資料不可混算**。切分工具已經現成：`prompt.py::rule_version()` 的內容雜湊，改 prompt 一個字就變，signals 表已有 `rule_version` 欄位。**一律用它切。**
- 🔴 **284 筆中性訊號的 entry_price 回填是 DB 寫入**（UPDATE 既有列的 NULL 欄位）。本輪沒做。這落在 N9 的 UPDATE 條款範圍，**要丹尼爾點頭**。回填本身是可重跑的（entry_date 已存在，價格從 yfinance 抓，免費）。
- 🔴 **`ai-trader` 的 ④股癌訊號組拿 signals 表當策略輸入**（`prompt.py` 的規則版本追蹤註解裡寫明）。改 prompt 前要先通知那邊，否則它會吃到 action=null 的新資料。

**風險**：
- 「方向量不出價值」有可能是**方向欄位本身標得不準**（盲判一致率只有 65.0%），不等於「多空這件事沒價值」。**拿掉標多空 = 放棄改進它的機會。** 這是本提案最大的認知風險，必須寫給丹尼爾看。緩解方式：欄位保留、只是不再要求模型努力判斷，日後想回頭仍有路。
- 改 prompt 後第一次實跑要燒 Gemini 額度（付費，需事前報價）。

**怎麼驗收**：
```
python -X utf8 update.py --dry-run          # 只列清單、不呼叫 Gemini，確認流程不炸
python -X utf8 -c "from prompt import rule_version; print(rule_version())"   # 確認雜湊已變
# DB 端一律先跑唯讀 dry-run 列出受影響的 284 筆，給丹尼爾看過再說
```
再挑 **1 集**用 `batch.py` 實跑（花錢，先報價），比對輸出 JSON 的 action 欄位確實 optional 且標的沒漏抓。

**能不能回滾**：
- 程式：能，`git checkout` 即可。
- 已寫入的新訊號：能，用 `rule_version` 篩掉。
- **entry_price 回填：不能自動回滾**（原值是 NULL，寫進去就沒有「原值」可還）——但影響是「多了資料」不是「壞了資料」，且 `backups/backup_stocksignal_db_2026-08-08.json` 有完整快照。

---

## L3 — 產品定位改寫（對外，不可回復）

**改哪些檔**：`welcome_email.py`（`股癌訊號勝率追蹤`、`歡迎訂閱｜股癌訊號勝率追蹤週報`、`最新一集提到的標的、方向與節目原文引用`）、`notifier.py`（`subject = f"【股癌訊號追蹤】{title}  勝率 {stats['win_rate']}%`）、`report_html.py`（三頁 header 的產品名、`_render_onboarding` 的 bullets）

**改成什麼**：產品名與信件主旨從「勝率追蹤」改成名單導向；訂閱承諾從「標的、方向與原文引用」改成「本週上榜名單、新進榜、掉榜、原文引用」。

**風險**：🔴 改產品名會出現在既有訂閱者的信箱裡，且與過去所有寄出的信不一致。歷史任務書（`stock-signal_TASK_*.md`）**不改**，保留歷史紀錄。

**怎麼驗收**：`python -X utf8 welcome_email.py --dry-run --no-send --no-add` 產出 HTML 目視；`grep -c "勝率追蹤" welcome_email.py report_html.py notifier.py` 應歸零或只剩刻意保留處。

**能不能回滾**：程式能，**已寄出的信不能**。

---

# 第四部分：誠實標出不確定的

## 4-1 需要丹尼爾再拍一次板的（不是我能決定的）

| # | 事項 | 為什麼要你拍板 |
|---|---|---|
| 1 | **L1 改週報信件版面** | 會改到訂閱者實際收到的信 = 對外發送、**不可回復** |
| 2 | **L3 改產品名與信件主旨** | 同上，且改的是產品身分 |
| 3 | **284 筆中性訊號回填 entry_price** | **DB 寫入**（UPDATE），落在 N9 條款；需先跑唯讀 dry-run 給你看 |
| 4 | **改 prompt 後實跑 Gemini 驗證** | **付費 API**，需事前報用途與費用 |
| 5 | **停用 `crosscheck.py` 的方向交叉驗證** | 它現在是唯一的品質防線之一，停掉等於降低品質稽核強度 |
| 6 | **通知 `ai-trader` ④股癌訊號組** | 它吃 signals 表；action 變 optional 會影響它的策略輸入 |
| 7 | **「拿掉標多空」還是「先修標多空」** | 方向盲判一致率只有 65.0%——量不出價值可能是**標得不準**。放棄前值得再想一次 |

## 4-2 我沒能驗證的宣稱（照實列）

1. **沒有連線資料庫。** 所有母體數字（973 / 284 / 638 / 51 / 215 / 266）來自 `backups/backup_stocksignal_db_2026-08-08.json` 這份 **2026-08-08 快照**。線上 DB 現況**可能已不同**——serenity-clone 在 2026-08-24 已把 32 筆代號修正寫進 DB。
2. **沒有重跑 serenity-clone 的回測。** 所有 56.34 / 48.17 / 54.15 / p 值都是從它的 JSON 正本與 PROGRESS.md **引用**，不是複驗。（我有做的是：跨 md 與 json 兩處對帳，並抓出 `p=0.0005` 查無來源。）
3. **「標多空是最費力的欄位」未經量化驗證。** 這是 `SOLOMON_HANDOFF.md` 的判斷。我從 `prompt.py` 的結構佐證（7 條 Rule 有 4 條在講多空、few-shot 也在示範多空判定），但**沒有量測 token 成本或 Gemini 帳單佔比**。「最費力」目前是結構推論，不是帳單數字。
4. **沒有實跑 `notifier.py` / `update.py` / `welcome_email.py`。** L1/L2/L3 的驗收步驟是設計出來的，**未執行過**。
5. **沒有用瀏覽器開過 `report_detail.html` / `report_attention.html`。** 我用 grep 確認它們含多空字串，但沒有目視版面，所以「方向 chip 是主視覺」是讀 CSS/JS 推斷的。
6. **沒有查 `_site/` 的線上部署現況**（本機 `_site/` 只有逐字稿 txt，是建置中間產物）。
7. **`docs/` 底下 6 份文件與多空無關**（都是逐字稿聽寫與外審），已逐檔看過檔名並 grep，未發現需要改的地方——但 `docs/host_idiom_glossary.md` 有「看好／看壞」字樣，那是語言習慣詞典，**與產品定位無關，不在變更清單內**。

---

# 覆蓋聲明

- **涵蓋什麼**：①`serenity-clone` 的 56.34% / 48.15% / 48.17% / 54.15% / +8.2pp / +2.2pp / +0.4pp 與四個 permutation p 值的定義、母體、期間與字串座標，含 `p=0.0005` 的查無來源結論 ②`stock-signal` repo 內 133 個追蹤檔中「標多空／`action`」的完整 footprint（逐檔逐用途，31 項）③L1/L2/L3 三層變更清單，每層含改哪些檔、改成什麼、風險、驗收、回滾 ④7 項待丹尼爾拍板、7 項未驗證宣稱。
- **未涵蓋什麼**：線上 PostgreSQL 現況（**全程未連線**，母體數字取自 2026-08-08 備份快照）；serenity-clone 回測的複驗（僅引用其 JSON 正本，未重跑）；「標多空最費力」的 token／費用量化；`notifier.py`／`update.py`／`welcome_email.py` 的實跑驗證；`report_detail.html`／`report_attention.html` 的瀏覽器目視；線上 GitHub Pages 部署現況；未追蹤檔（`_site/`、`transcripts_data/`、各 `*.log`）；丹尼爾說先不動的兩題（`stock_dict` 6 筆代號、初始資金）。
- **來源檔案與版本**：`D:\All claude\300_Projects\stock-signal`（git `master`，HEAD `077a00d`，133 個追蹤檔）的 `prompt.py`／`analyzer.py`／`database.py`／`performance.py`／`report.py`／`report_html.py`／`notifier.py`／`welcome_email.py`／`attention.py`／`line_query.py`／`split_sentinel.py`／`crosscheck.py`／`check_site_payload.py`／`.github/workflows/*.yml`／`backups/backup_stocksignal_db_2026-08-08.json`；`D:\All claude\300_Projects\serenity-clone` 的 `SOLOMON_HANDOFF.md`（10,520 B，269 行）／`PROGRESS.md`（574 行）／`data/significance.json`／`data/stockpick_significance.json`（seed 20260824）；`D:\All claude\000_Agent\001_memory\daily\2026-08-24.md`。
- **查證時間**：2026-08-24（本機檔案 mtime 與 git HEAD 實查；**本輪對外部服務／資料庫零呼叫**）。
