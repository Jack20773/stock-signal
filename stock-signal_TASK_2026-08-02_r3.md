# stock-signal 索羅門任務檔 — 2026-08-02 第三輪（獨立轉錄逐字稿來源：交叉驗證 + 缺口補齊 + 可重用觸發腳本）

> 讀完本檔前，先讀通用章程 `D:\All claude\000_Agent\006_institution\pm_agent_solomon.md`。
> 這輪任務跟 `stock-signal_TASK_2026-08-02_r2.md`（分歧仲裁+詞典研究+報告介面3項）完全獨立，已完工上線，不用理會。
> `stock-signal_TASK_2026-08-03.md` 是更早的過期死檔（內容已在索羅門第三輪卡片化任務完工），不用理會、不要沿用其中任何內容。
> **本輪絕對紅線維持章程原本的五類**（push / 對外發送 / 碰密鑰 / 正式DB破壞性寫入 / 刪使用者原始資料一律停手問人），r2 那次「縮小成僅剩花錢」是使用者明確裁決過的**一次性例外，不適用本輪**。

## 0. 這輪任務怎麼來的

主控 session 跟使用者討論「逐字稿來源依賴第三方網站（`whatmkreallysaid.com`）」的風險——對方不更新，我們就永遠拿不到新集數，這是 `download_transcripts.py` 目前唯一的資料來源。

查證確認：`video-transcribe` 專案（`D:\All claude\300_Projects\video-transcribe\`）已經有本地獨立轉錄能力（`yt-dlp` 下載 + `faster-whisper` 轉錄），2026-07-31 曾用 EP680 做過真人測試，跟 `whatmkreallysaid.com` 官方逐字稿比對字元相似度 93.74%，差異主因是官方稿省略主持人開場口白，不是辨識錯誤——品質已驗證堪用。

主控 session 也已用 WebSearch 確認股癌官方 YouTube 頻道存在：**`https://www.youtube.com/@Gooaye`**（頻道 ID `UC23rnlQU_qE3cec9x709peA`），影片標題含 EP 編號（如「Gooaye 股癌- EP629」），編號系統跟 `stock-signal/transcripts/` 現有檔名（如 `EP680_xxx.md`）對得上。

使用者裁決這輪範圍：
1. 交叉驗證第一批只做**最新 2 集**（不 backfill 全部 680 集）
2. 順便把「我們跟 `whatmkreallysaid.com` 雙方都缺」的集數用獨立轉錄補上（已知至少 EP677 疑似此類缺口，需先查證是否真的雙方都缺）
3. 產出一支**可重用的觸發腳本**，作為未來「股癌發新影片時自動下載+轉錄+套用進系統」這個長期目標的地基——但這輪**不**設定實際的常駐排程（Windows Task Scheduler 等），那是下一步要另外跟使用者確認的決定

## 1. 任務清單

### 1a. 格式橋接：video-transcribe 輸出 → stock-signal .md 格式（地基，其餘任務都依賴這個）

**目的**：`transcribe.py` 輸出的是帶時間軸的 `.srt`/`.ass`，`analyzer.py` 吃的是純段落文字 `.md`（比照 `transcripts/EP99_焦慮bad.md` 的格式：純文字段落、`---` 分隔小節，無時間軸、無 metadata header）。

- 新增轉換函式（建議放進 stock-signal 新模組，例如 `independent_transcribe.py`），輸入 `.srt` 路徑，輸出符合現有 `.md` 格式的純文字（去時間軸、去序號、合併同段落發言、保留自然分段）
- **不要修改 `video-transcribe` 專案任何檔案**——只能把它的 `transcribe.py` 當外部 CLI 工具呼叫（`subprocess`），讀它產出的 `.srt` 檔案做轉換，這是唯一允許跨到該專案目錄的方式（讀取其輸出，不編輯其原始碼）
- 驗證：轉換後的 `.md` 跟既有 `whatmkreallysaid.com` 版本相比，格式上要讓 `analyzer.py`/`batch.py` 能正常讀取解析（不需要一致的用詞，但檔案結構要相容，實際跑一次 `analyzer.py` 對轉換後的檔案不出錯即為驗證通過，注意這是**測試轉錄品質不是要真的存入 DB**，用 dry-run 或不接資料庫的呼叫方式驗證，避免觸發真正的 Gemini 付費分析)

### 1b. 頻道集數列表 + 缺口偵測

- 用 `yt-dlp --flat-playlist` 對 `https://www.youtube.com/@Gooaye` 取得完整影片清單（標題含 EP 編號），解析出 EP 編號 → YouTube 影片網址的對照表
- **先驗證標題解析的可靠性**：抽查至少 10 筆確認 EP 編號能穩定從標題解析出來，如果格式不一致或有例外（例如特別企劃集數沒有編號），要在報告裡明確列出無法解析的案例，不要靜默跳過
- 比對三份清單：①YouTube頻道實際集數 ②`stock-signal/transcripts/` 本地既有 `.md` 檔案 ③`whatmkreallysaid.com` 的 `episodes.json`，找出「YouTube 有、但本地跟對方都沒有」的集數（已知 EP677 疑似此類，需要先直接查證 `whatmkreallysaid.com` 對 EP677 是否真的 404／缺失，不能只看我們本地沒下載到就假設對方也沒有）
- 產出清單寫進報告，即使結果是「只有 EP677 一筆」或「意外發現不只一筆」都如實列出

### 1c. 缺口補齊：獨立轉錄補上雙方都缺的集數

- 對 1b 找出的缺口集數（預期數量很小），用 `video-transcribe` 下載+轉錄+套用 1a 的格式轉換，寫入 `stock-signal/transcripts/` 對應檔名
- 因為沒有既有版本可比對，這些是**直接採用**獨立轉錄結果（跟 1d 的「有基準可比對」不同），完工報告要清楚標注「這幾集是純獨立轉錄來源，沒有第二方交叉驗證」
- 同步更新本地 `episodes.json`（如果該工具用得到這個清單的話，先確認 `download_transcripts.py`/`performance.py` 讀取 `episodes.json` 的邏輯，補進去的集數要能被既有 pipeline 正確識別，不要造成後續 `analyzer.py`/`batch.py` 讀到不存在於清單裡的孤兒檔案）

### 1d. 交叉驗證：最新 2 集

- 用 1b 找到的最新 2 集 YouTube 影片，跑獨立轉錄 + 1a 格式轉換
- 跟 `whatmkreallysaid.com` 既有同集數 `.md`（如果這 2 集對方已有）逐段落比對差異，**只記錄不覆蓋**：新增一個純附加的紀錄方式（新表或 `docs/` 底下的比對報告文件皆可，索羅門自行判斷，若用 DB 新表屬純附加 DDL，不用停手，dry-run 後可直接執行），欄位/內容至少要包含：集數、差異段落、差異類型（漏字/口白省略/實質內容不同）
- 如果這 2 集剛好 `whatmkreallysaid.com` 也還沒有（對方可能還沒更新），就自動併入 1c 的「缺口補齊」流程處理，不用勉強做比對

### 1e. 可重用觸發腳本（為未來「新集數自動處理」鋪路，這輪只交工具不交常駐排程）

- 把 1a-1d 的邏輯整合成一支參數化腳本（建議 `sync_independent_transcripts.py`），至少支援：
  - 讀取頻道最新集數，跟本地/對方清單比對，自動判斷該集要走「缺口補齊」還是「交叉驗證」邏輯
  - 可重複執行（已處理過的集數要跳過，比照 `download_transcripts.py` 現有的 `SKIP` 邏輯）
  - 清楚的 log 輸出（比照現有 `download_transcripts.py` 的 `[編號] OK/SKIP/FAIL` 格式風格）
- **明確不做**：不要設定 Windows Task Scheduler 或任何常駐排程讓這支腳本自動定期執行——這涉及讓使用者的電腦被排程反覆執行本地轉錄，屬於需要使用者明確拍板的資源使用決定，這輪只需要證明「手動跑一次這支腳本可以正確處理新集數」，自動觸發的時機與方式留給使用者看過這輪成果後再決定
- 完工報告要清楚寫「這支腳本目前需要手動執行，之後如果要接自動排程，需要另外跟使用者確認執行頻率與方式」

## 2. 明確排除（這輪不做，避免自作主張擴大範圍）

- 不 backfill 全部 680 集的獨立轉錄——只處理 1b 找到的真實缺口 + 1d 的最新 2 集
- 不設定任何常駐/自動排程觸發（Windows Task Scheduler、cron 等）——只交出可手動執行的腳本
- 不修改 `video-transcribe` 專案任何檔案——只能把它當外部工具呼叫
- 不修改 `analyzer.py`/`batch.py`/`update.py`/`database.py` 既有分析 pipeline 的核心邏輯——這輪只處理逐字稿來源這一段，不動下游分析
- 不覆蓋任何 `whatmkreallysaid.com` 來源的既有 `.md` 檔案內容——交叉驗證只記錄差異，不動既有檔案

## 3. 範圍限定

新增程式碼只能寫在 `D:\All claude\300_Projects\stock-signal\` 目錄底下。唯一允許的跨專案動作是把 `D:\All claude\300_Projects\video-transcribe\transcribe.py` 當外部 CLI 呼叫（subprocess），**不得編輯該專案任何檔案**。例外只有依通用章程要追加的記憶檔。

## 4. 監督模式與截止時間

- **監督模式：無人監督**（使用者裁決，2026-08-02）。真正卡住的子任務標記 `blocked` 寫進報告，不依賴它的其餘任務繼續做，不要整個停擺。
- **截止時間：2026-08-02 23:09**（任務檔建立於 19:09，使用者指定「4 小時內」）。
- 建議施工順序：1a（格式橋接，地基）→ 1b（缺口偵測，先弄清楚要處理哪幾集）→ 1c（缺口補齊）→ 1d（最新2集交叉驗證）→ 1e（整合成可重用腳本，放最後因為前面都驗證過才知道怎麼整合合理）。

## 5. 完成的定義

1. 1a：實際轉換至少一份 `.srt` 成 `.md`，貼出轉換前後對照片段，並證明 `analyzer.py` 能正常解析轉換後的檔案（不需真的存入 DB）
2. 1b：貼出完整的 EP 編號解析驗證結果（抽查筆數+成功率）、貼出找到的缺口集數清單（或「沒有找到缺口」的查證過程），EP677 是否真為雙方都缺要有明確查證結論（不是假設）
3. 1c：缺口集數已實際補進 `transcripts/`，貼出檔案內容片段，並確認 `episodes.json`/既有 pipeline 能正確識別新增檔案（例如跑 `batch.py --dry-run` 確認新集數被列為「待分析」而非被忽略）
4. 1d：最新 2 集的比對結果貼出（差異內容或「無差異/對方也還沒有」的結論），比對紀錄有實際寫入並查詢驗證存在
5. 1e：`sync_independent_transcripts.py` 至少完整跑過一次，貼出實際 log 輸出；明確聲明「未設定自動排程，需手動觸發」
6. Codex 若有使用（例如疑難排解時二意見審查）依既有 grep balance 方法回報花費，50點標準預算內
7. 完工報告清楚標注每一項「真的完成」vs「待使用者審閱」的部分，尤其 1c 補進去的缺口集數內容、1d 的差異比對結果，都是要給使用者看過的重點
