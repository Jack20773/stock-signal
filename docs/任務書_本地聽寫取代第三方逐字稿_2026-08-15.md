# 任務書：本地聽寫取代第三方逐字稿

> **這份怎麼用**：新 session `/clear` 後直接讀這份就能動手，不用回頭問丹尼爾要不要做。
> 只有在「做法有分岔且影響後續架構」或「要花錢」時才停下來問他。
> 建立於 2026-08-15。來源＝當日實測 ＋ Codex(high) ＋ DeepSeek 雙審（兩家意見已歸納，**不要重跑審查**）。

---

## 一、目標（丹尼爾原話與拍板事項）

原話：「**下次你分析你之前的然後跟官方逐字稿比，把專有名詞的弱點修正，之後取代他**。」

1. 逐字稿目前唯一來源是第三方站 `whatmkreallysaid.com`，對方停更／關站就斷炊。
2. 本地已有聽寫管線（yt-dlp + faster-whisper `large-v3-turbo`, CUDA），**內容完整但專有名詞會錯**。
3. 拿官方稿當**對照答案**，系統性找出並修正專有名詞弱點。
4. **終點是取代官方站**，不是永久混合。混合只是過渡手段。

**已拍板、不用再問**：走這條路、混合是過渡、順序照本任務書。
**必須問他才能定的**：P6 的取代門檻數字（下方有雙審建議值，但要他點頭）。

---

## 二、已完成，不要重做

| 項目 | 狀態 |
|---|---|
| 本地聽寫管線 | 已通，端到端實測過。`independent_transcribe.py` ＋ `sync_independent_transcripts.py` |
| video-transcribe 編碼 bug | 已修 `a461c89`（yt-dlp 用 gbk 寫 stdout、中文路徑變亂碼，8/12 起壞三天） |
| 工作目錄路徑不相容 | 已修 `587ae81`（新版收進 `_工作檔/<name>/`，改成新舊都找） |
| 配對素材保全 | **`docs/pairing_samples/`（已進版控）**：EP681–684、EP687 的 **原始 .srt（含時間軸）** ＋ EP687 聽寫版 .md。對應的官方版在 `transcripts/`（EP681–684 各兩份，另一份就是我方版） |
| 測試影片清理 | 766MB 已刪（丹尼爾授權），字幕原始檔全數保留 |
| 雙審 | Codex US$0.21 級距 ＋ DeepSeek US$0.008，原文已存 docs/，**不要重跑** |

⚠️ **`transcripts/` 裡 EP681–684 各有兩份 .md（官方版＋我方聽寫版），那是配對素材，不准當重複檔刪掉。**
官方版檔名以 `episodes.json` 的 `filename` 為準，另一份就是我方聽寫版。

---

## 三、已查證的事實（引用前不用重查）

- **官方稿是 silver label 不是 gold**：來源站自己聲明逐字稿「透過人工智慧轉譯、**人工修正**與格式化整理而成」。
  → 專有名詞可當對照答案，但**逐字差異不能全算成我方聽錯**（對方也潤稿、可能刪業配）。這決定了 P2 必須先做前提驗證。
- **聽寫效能**：EP687 實測 **261 秒/集**（RTX 5070 Ti，large-v3-turbo, cuda），下載 77MB。
- **內容完整度沒問題**：去標點後我方 19,837 字 vs 官方 19,607 字（我方還多 1.2%），開頭結尾都對得上，無漏段。
- **弱點在專有名詞**（EP687 訊號最密集那段，主持人一次點名 5 檔）：

  | 正確 | 我方聽成 | 類型 |
  |---|---|---|
  | 台積電 / 台達電 | **臺**積電 / **臺**達電 | 異體字 |
  | 奇鋐 | 奇虹 | 同音誤聽 |
  | 貿聯-KY | 茂林KY | 誤聽＋格式 |
  | 載板 | 載版 | 同音錯字 |
  | 股癌 / 謝孟恭 | 古來 / 聖夢公 | 節目名、人名 |

  該段 5 檔只有 `0050`、`智邦` 會被正確識別。
- **段落級 difflib 粒度就是錯的**：`compare_paragraphs()` 對 EP687 給 88.34% 相似度卻**只回報 1 個 diff block**。雙審一致：段落級只能當「哪裡大範圍不同」的粗篩，抽錯誤詞對必須做到詞／字元層級。
- **標準答案不限量**：官方站有 687 集，任一集都能自己聽寫產生配對，成本只有 GPU 時間。
- `stock_dict.py` 只 export `resolve()` / `resolve_code()`，**沒有可列舉的股票名稱清單**，要 refactor 才能導出。
- `video-transcribe/transcribe.py` **沒有** `initial_prompt` / hotwords 支援，要加得自己改。

---

## 四、絕對紅線（派分身時必須原文轉貼這一節）

1. **不准刪檔**：任何刪除一律先列清單給丹尼爾。`transcripts/`、`cross_validated_raw/`、`episodes.json` 尤其不准動。
   （2026-08-14 有分身用 `rm -rf` 刪掉跨 session 的資料夾未列清單，教訓在此。）
2. **不准動 `signals` 表既有資料**。新增欄位／新表可以（純附加），UPDATE／DELETE 一律先唯讀 dry-run 給丹尼爾看。
3. **付費 API 先報用途與費用**。P0–P4 完全不需要付費 API；P5、P6 要用 Gemini，動手前先報。
4. **不准用 `str.replace()` 做全域錯字替換**（雙審共同點名的最大風險）。
5. **不准把 withheld／locked test 的集數拿去挖規則、調門檻或調 prompt**。第一天就鎖死。

---

## 五、執行階段

### 🔴 P0：先把官方站 687 集整包備份下來（**最先做，今天就做**）

**DeepSeek 判定 critical，理由是整個計畫都靠「官方稿拿得到」，站倒了所有驗證同時斷炊。**
這件事跟演算法無關、幾十分鐘就能做完，**不要排在任何開發工作後面**。

1. 抓 `whatmkreallysaid.com` 全部 687 集逐字稿 ＋ metadata（集數／日期／標題／檔名）。
   既有的 `download_transcripts.py` 已經在做這件事（本機 `transcripts/` 已有 687 集），**確認完整性即可，不用重造輪子**。
2. 另存一份原始 HTML/JSONL 快照（含抓取時間、來源 URL、內容 hash），與清洗後版本分開放。
3. 之後每週增量抓新集。

**DoD**：687 篇全數在案且字數落在合理區間、標題日期可核對；**官方站明天消失也不影響後續所有階段**。

---

### P1：把時間軸救回來（其他階段都靠它）

**為什麼**：`srt_to_md()` 產出的 .md 沒有時間軸，等於丟掉「回頭聽音訊消歧」的唯一證據。Codex 判定這是目前最該補的結構缺陷。

1. 改 `independent_transcribe.py`：`.md` 保留，但**額外輸出 `segments.json`**（每段 `segment_id` / `start` / `end` / `text`，有的話含 word timestamp）。
2. 新增 `asr_run_metadata.json`：模型、版本、語言、beam、VAD、prompt、設定 hash。
   **理由**：ASR 設定一變，之前挖到的錯字規則就可能失效，沒這份 metadata 事後分不清。
3. 既有 5 份 `*_raw.srt` 已有時間軸，補做成同格式即可，不用重新聽寫。
4. **順手做**：yt-dlp 下載音檔時加 `--write-auto-sub` 抓 YouTube 自動字幕當**第三來源**。零成本，之後可做三方多數決消歧（DeepSeek 建議，划算）。

**DoD**：任一段本地文字都能回查到對應音訊時間範圍。

---

### P2：驗證「官方稿能不能當標準答案」（便宜，先做完再開發對齊器）

官方稿是 AI 轉譯＋人工修正，**不是無條件真值**。先用最便宜的方式驗前提：

1. **抽 3–5 集，把官方稿裡的股票／ETF 名稱逐一比對證交所／櫃買中心正式掛牌名**（公開資料可批次取得）。股票名是有限集合，一天內可做完，而且正好是下游最在乎的類別。
   - 官方實體準確率 **≥ 99%** → 放心當標準答案。
   - 只有 **90–95%** → 把「跨集複現」門檻提高到 ≥3 次，且只信任同音配對。
2. 抽樣人工聽音建小型 gold set，優先抽：股票密集段落 ＞ 官方／我方差異最大段落 ＞ 已知同音誤聽段落。跨不同年份抽，不要都抽最近的。
3. 統計官方稿的：股票實體正確率、人名／節目名正確率、**潤稿比例、有沒有整段刪掉業配**。

**DoD**：有一份能回答「官方稿可不可以當 silver label」的審計報告。**沒做完之前，不准把官方稿當無條件訓練真值。**

⚠️ **若查出官方會刪業配段落**：我方稿會多出官方沒有的整段，`phantom rate` 會被贊助商名字灌爆。對齊輸出必須把「疑似官方刪減段落」標記並排除，否則後面所有指標都是髒的。

---

### P3：對齊器（PoC 用現有 5 組配對）

**不要改** `compare_paragraphs()` 的行為，**新增**一支 episode 級對齊器。雙審都給了同一個方向：

1. **正規化**只做低風險處理：全半形、空白換行、標點、`KY`／`-KY` 格式、`台`／`臺` 建立**比對用等價規則**。
   ⚠️ 原文一律保留；正規化函式要保留「正規化後索引 → 原文位置」映射，否則找到差異也回報不了位置。
   ⚠️ **不要**一開始就把所有同音字轉成同一字，會把不相干片段硬對上。
2. **分層對齊**，不要對兩萬字裸跑 difflib：
   - 先找共同的長字串 n-gram 當錨點（8～15 字，忽略出現太多次的，只採順序單調的錨點鏈）
   - 錨點之間切成局部窗，窗內用字元／詞級全域動態規劃（Needleman-Wunsch／edit distance，支援增刪改）
   - 標點空白成本設低或 0，一般中文字成本 1
3. 每個差異 span 輸出：官方原文、我方原文、前後 20～50 字上下文、對齊信心、**對應的 SRT segment ID 與時間範圍**。
4. 產出人看得懂的 HTML/Markdown 差異報告。

**DoD（要有鑑別度，不是跑得動就算）**：
- 已知案例必須各自成為獨立 span，不再是一個巨大 diff block：`台/臺積電`、`奇鋐/奇虹`、`貿聯-KY/茂林KY`、`載板/載版`、`股癌/古來`、`謝孟恭/聖夢公`
- 每個 span 有對齊信心與音訊時間範圍
- 對不上的區段標記 uncertain，不准硬配對

---

### P4：股票實體詞典 ＋ 候選誤聽規則庫（**不做全域替換**）

**4a 詞典**
1. 擴充 `stock_dict.py` 或新增 `stock_entities.py`，提供可列舉的實體集合（canonical 名稱／代碼／別名／格式別名如 `-KY`／`台`≡`臺`）。
2. 股票、人名、節目名、產業術語**分開存**，不要混成一張表。
3. 新增 `resolve_text_to_candidates()`：回傳**零到多個**候選，一個別名對到多檔時不准任選一檔。

**DoD**：可列出完整股票實體集合；別名衝突會回多候選；詞典有版本號與測試案例。

**4b 規則庫**

雙審共同警告：**全域「錯→對」字串替換必然誤傷**——看到「奇虹」時，光憑文字無法知道該不該改成「奇鋐」（可能某集真的講奇虹）。**誤傷造成的假訊號比漏訊號更嚴重**（漏一筆只是少算，假訊號會把不存在的交易算進勝率）。

規則分四級，預設從嚴：

| 級別 | 意思 | 例子 |
|---|---|---|
| `safe_normalize` | 可無條件自動修 | 臺→台 等異體字、`-KY` 格式 |
| `entity_link_only` | **不改原文**，只在實體連結時視為同一檔 | 奇虹 → 連到 奇鋐 |
| `candidate_only` | 只標記候選，**交給下游 Gemini 在既有呼叫內消歧**（不額外花錢，DeepSeek 建議） | 高風險同音詞 |
| `rejected` | 驗證後判定不可用 | |

每條規則要有 rule ID、證據（出現在幾集／幾次／對齊品質／人工驗證數）、**適用的 ASR 設定**。
**跨集複現是防線中的防線**：只出現一次的配對不得升級成 `safe_normalize`。

**DoD**：程式碼裡不存在任何全域 `str.replace()` 形式的錯字替換；每次校正都可追溯到 rule ID。

---

### P5：評估器與抽樣（本地為主，先不花錢）

**5a 本地評估器（零 API，每集都能跑）**
- **entity recall**（mention 層級，模糊匹配後）：官方稿每個實體 mention，我方稿能對到的比例。
- 🔴 **phantom entity rate**：我方稿有、官方稿沒有的實體 mention 比例。**這是最重要也最危險的數字。**
- **episode-level agreement**：單集「官方實體代碼集合」與「我方實體代碼集合」完全一致的集數比例。
- 另計：canonical 連結正確率、校正誤傷率。
- 報告要能分開看：原始 ASR ／ 只做安全正規化 ／ 加候選校正 ／ 加 prompt ／ 局部重解碼。

⚠️ **不要用字元或詞相似度當 gate**——已經證明沒鑑別度（88% 相似卻 5 個專有名詞全錯）。
⚠️ **不要用「差異數」當門檻**——不同集語速與口語密度差很多。

**5b 抽樣策略**

| 階段 | 集數 | 選法 |
|---|---|---|
| 建表 | 現有 5 集 seed ＋ 再抽 **25–30 集** | 687 集分層（每 50 集一層約 14 層），每層隨機 1–2 集；實體 mention 數 Top 20% 的訊號密集集加權多抽；**有來賓的集數要多抽**（會帶進不同領域的公司與人名） |
| **withheld 驗證** | **10–15 集**，建表期間完全不看 | 5 集來自最新 100 集、5 集來自其他層。**不得用來調任何門檻** |
| rolling 監控 | 每 20 集新增 1 集 | 新公司／新 ETF 一直出現，校正表要持續更新 |

**停止條件**（不是拍腦袋定集數）：每新增一集，新出現的「前所未見錯→對配對型別」數 < 1，且 Top 500 實體的逐實體錯誤率變動 < 1%。
粗估 **建表 30 集 + 驗證 10 集 ≈ 40 集 ≈ 3 小時 GPU**。

**5c prompt / hotwords A/B**
四組對照：baseline ／ 只餵節目名人名 ／ 小型高頻股票詞典 ／ 局部候選詞重解碼。
⚠️ **不要一次把完整股票清單塞進 `initial_prompt`** ——會增加幻覺（憑空生出沒講的股票）。
⚠️ **若 prompt 提升 recall 卻讓 precision 或 phantom rate 變差，禁止上線。**

---

### P6：取代決策（要花 Gemini，動手前先報丹尼爾）

**雙審建議的門檻值（保守，最終要丹尼爾拍板）**：

| 指標 | 建議門檻 |
|---|---|
| entity recall（mention 層級、模糊匹配後） | ≥ 98% |
| **phantom entity rate** | **≤ 0.2%**（每 1000 個 mention 最多 2 個） |
| episode-level agreement | ≥ 95%，且在**最近連續 30 集 rolling window** 上維持 |
| L1 訊號層方向一致率 | ≥ 97%，且**零「我方獨有假訊號」** |

phantom 門檻比 recall 嚴的理由：**傷害不對稱**——漏一個訊號只是少算一筆，假訊號會把不存在的交易算進勝率。

**訊號層驗收做法**：同一集，官方稿與我方校正稿各跑一次 Gemini，比對「股票代碼→多/空方向」pair 的一致率。每月抽 10 集即可。
⚠️ **Gemini 對 Gemini 的一致性不是正確性證明**，不一致的個案要人工聽音裁決。

**四個條件同時滿足才能宣告可取代**：實體 precision/recall 達標、無高風險自動校正誤傷、訊號一致率達標、人工 gold set 驗證通過。

---

## 六、雙審沒解決、但要記得的事

- 🔴 **取代官方站 ≠ 完全自主**：我們仍然依賴 YouTube（yt-dlp 抓音檔），只是把單點依賴換一家。要真正自主得自己存音檔。
  **這點要主動跟丹尼爾講清楚**，他可能以為做完就沒有外部依賴了。
- **模型或設定一變，既有錯字規則可能全部失效** → P1 的 metadata 是必要的，不是可有可無。
- **long-tail 補不完**：只出現一兩次的新公司名學不到，只能靠下游消歧 ＋ rolling 更新。「取代」的承諾會卡在長尾，先講清楚別過度承諾。
- 錯字表與 prompt **不等於**訓練模型，別對外講成「訓練我們自己的模型」。

---

## 七、順帶待辦（跟本任務書無關但別忘）

- **686、687 官方稿已下載到本機，但正式站仍停在 EP685**。要更新得跑一次 `update.yml`，**會順帶寄週報信**給 `REPORT_TO` ＋ `extra_recipients.txt`（含丹尼爾同事）。丹尼爾一直沒回要不要手動觸發，**不要自己按**。
- `video-transcribe/review4_codex_run.log`（53KB，8/12 外審紀錄）去留未定，等丹尼爾決定。

---

## 八、外部審查原文

- Codex（gpt-5.6-terra, effort=high）：`docs/外審_Codex_本地聽寫取代方案_2026-08-15.md`
- DeepSeek（deepseek-v4-flash，US$0.008，28,162 completion tokens）：`docs/外審_DeepSeek_本地聽寫取代方案_2026-08-15.md`
- 送審題目：`docs/送審題目_本地聽寫取代方案_2026-08-15.md`

**兩家都是非盲測**（都看得到我的診斷與傾向），依制度只能說「在我提供的背景下，其結論與我一致」，**不得稱獨立證實**。
兩家分歧不大，主要差別：DeepSeek 把「備份官方站」列為 critical 且要求最先做（已採納為 P0），Codex 對評估指標與 train/test 隔離講得更細（已採納進 P5/P6）。
