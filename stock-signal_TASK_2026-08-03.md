# stock-signal 索羅門任務檔 — 2026-08-03（已派工，30分鐘穩定性試跑）

> **狀態：已派工**——使用者 2026-08-01 22:52 裁決先跑 30 分鐘短窗口測試目前索羅門機制穩定性，不是直接衝完整 3-4 小時。監督模式與截止時間見第 6 節。
> 讀完本檔前，先讀通用章程 `D:\All claude\000_Agent\006_institution\pm_agent_solomon.md`。

## 0. 這輪任務怎麼來的

2026-08-01 使用者看過 C 類 9 項改動的實際畫面後說「不是我要的」，真正想要的是報告介面體驗更新。2026-08-02 走完完整的「指派前產品思考流程」（目標→現況→選項→範圍），依序：
1. 用 Artifact 畫了方案 A（儀表板優先）/方案 B（個股卡片化）兩個視覺提案，使用者選 **方案 B**
2. 追問篩選工具列放法，用可互動 demo 比較「完整篩選列」vs「簡化篩選列」，使用者選 **簡化篩選列**
3. 使用者提出更大的新需求（「現在該關注哪些標的」的關注度排序），已裁決**拆成獨立計畫**（見 `100_Todo/projects/2026-08-02_stock-signal報告第二頁-關注度排序計畫.md`），不排入本輪
4. 討論 sparkline 資料來源限制（`price_cache` 只有單點快照無連續序列），使用者選擇**拉歷史價格畫真 sparkline**

以下技術設計已定案，可以直接排入索羅門施工清單。

## 1. 任務清單（技術設計已定案）

### 1a. 個股排行：表格 → 卡片網格
- `report_html.py::renderStockTab()` 改寫成渲染卡片網格而非表格列，每張卡片顯示：標的名稱+代號、市場 chip（台股/美股）、方向 chip（看多/看空）、報酬率（大字，紅漲綠跌沿用既有慣例）、sparkline、持倉天數、提及次數
- 所有文字欄位（標的名稱、代號等來自 Gemini 分析結果的自由文字）**必須套用既有 `escapeHtml()`**，比照 `renderDetailTab()` 現行防護，不可繞過
- 視覺參考：`https://claude.ai/code/artifact/84f24141-6250-4a52-ba9c-11a5270baac2`（方案 B 定案版demo，含可互動篩選）——**這是示意稿，不是可以直接搬的正式程式碼**：demo 用內嵌 `<style>`/純前端合成資料，正式版要整合進 `report_html.py` 既有的 CSS 變數系統與 Python 端資料組裝，不要整段複製 demo 的 HTML/CSS

### 1b. Sparkline：拉歷史價格
- `prices.py` 新增批次歷史價格序列函式（例如 `batch_get_price_series(tickers, start_date, end_date)`），**沿用索羅門第二輪剛建立的批次下載模式**（一次 `yf.download()` 涵蓋所有需要的 ticker+日期區間，不要退化回逐檔呼叫）
- 序列時間窗：**AI 暫定，需你核對**——建議用「進場日到今日」，若持倉超過 60 天則只取最近 60 個交易日（避免序列過長讓 sparkline 失去意義），這個窗口大小需要你看過實際渲染效果再拍板
- 這個價格序列**不寫入 `price_cache` 表**（該表是既有的單點快照設計，2026-08-02 剛裁決維持現狀不加 TTL，序列資料塞進去會打架）——序列以 JSON 直接內嵌進 HTML，比照 `SIGNALS_DATA` 現行做法
- **範圍提醒**：現行 `renderStockTab()` 涵蓋「以標的」tab 裡的**所有**已判定標的（不只前幾名），卡片化不能縮小涵蓋範圍，代表這個批次下載可能一次涵蓋數十~上百檔 ticker——批次呼叫設計要能撐住這個量級，不是只測小樣本

### 1c. 篩選工具列：簡化版
- 只保留：搜尋（標的名稱/代號）、市場切換（全部/台股/美股）
- **不做**：勝負篩選、持倉天數篩選（demo 裡的完整版功能，這次不採用，除非之後另外要求加回來）

### 1d. 依集數列表：保留，降級為次要區塊
- 原本的「每集訊號」表格保留，但從主要 Tab 降級成卡片網格下方的次要區塊，預設收合（比照 demo 的 `ep-compact`/`ep-toggle` 行為）

### 1e. 不動的部分（明確排除，避免自作主張）
- **Email 版報告（`generate_html_email()`）不改**，維持現有純 table 版本，不用同步卡片化
- **進階統計區塊（信心等級準確率、持倉時間分組勝率、趨勢圖）沿用現有收合行為**，不在這輪重新設計或搬動位置

## 2. 殘餘風險清單（來自 `SOLOMON_HANDOFF.md` 2026-08-02 版 `remaining_risk`，本輪視餘裕處理，非強制）

1. DB 寫入路徑全程 mock 驗證，沒有連線真實 DB 測過——部署後第一次跑建議用 `--dry-run` 或 `--last 5`
2. yfinance 對下市股/查無資料 ticker 的真實回傳形狀邊界情況沒驗證過（已驗證 AAPL/2330.TW/SPY 等知名股正常）
3. `analyzer.py` 結構化驗證只確保 `extracted_signals` 存在且是 list，list 內元素若非 dict 仍可能在 `database.py` for 迴圈對 `.get()` 出錯（Codex 標非阻塞建議）
4. 歷史價格回溯 10→20 天是「先求有再求精」版本，非真正休市日曆，極端連假仍可能抓不到價格
5. 沒有做過「跨所有第二輪項目一起跑一次 `update.py` 全流程」的整合測試

## 3. 舊檔案清理（候選，依索羅門自主決策範圍第4類判準自行判斷）

`verify_neon.py`、`verify_neon_updated.py`、`verify_report.js`（2026-08-02 清理殘留檔時發現、當時未列入清單）。索羅門依章程「自主決策範圍」第4類四項判準（範圍內/非核心資料/輔助性一次性檔案/`grep` 確認無程式碼引用）自行判斷，符合就走強制留痕機制（先 `git add`+commit 再 `git rm`）刪除；任一判準不確定就列清單交使用者處理，不要自己猜。

## 4. 已拍板、不用施工的決策（記錄用，避免之後又被重新挖出來問）

- 第8項（價格快取加 TTL）：**維持現狀**，不加
- 第11項（篩選時統計母體跟子集變動）：**維持現狀**，固定用全集

## 5. 範圍限定

只能動 `D:\All claude\300_Projects\stock-signal\` 目錄底下的檔案；例外只有依通用章程要追加的記憶檔。**不得**擴大處理「目前關注度」新功能（已拆到獨立計畫檔，未定案技術設計，不在本輪範圍）。

## 6. 監督模式與截止時間

- **監督模式：有人監督**——使用者這輪在場，卡住時可以直接問，不用套用無人監督的隔離阻塞流程；但判斷等待成本划不來、或沒把握使用者秒回，一樣可以直接套用無人監督做法，不用糾結。
- **截止時間：2026-08-01 23:22（開工起算 30 分鐘）**——這是使用者刻意選的短窗口，目的是先測試索羅門機制目前穩不穩定，不是要求 30 分鐘內做完全部 1a-1e。**時間到就停手寫 `SOLOMON_HANDOFF.md` 交接，不要為了趕完整清單而犧牲驗證品質或跳過 commit 紀律**——這次的「完成」標準是「30分鐘內的產出安全、可驗證、有老實交接」，不是「清單全部打勾」。
- 建議施工順序（時間有限，優先度由高到低）：先做 1a（卡片網格改版）最基礎的骨架 + `escapeHtml()` 防護，有餘裕再做 1b（sparkline 批次歷史價格），1c/1d 視剩餘時間彈性處理，不強求本輪做完。
- **版本錨點 commit hash：`243784b8e485f810be1c2864bd0863dc5803cee`**（本檔開工前的 commit，之後判斷是否偏離已定案技術設計以此為對照）

## 7. 完成的定義

1. 1a-1d 每一項都有實際驗證證據（跑過的指令+輸出），前端改動用 Playwright 真實瀏覽器驗證（索羅門第二輪已裝好 Playwright，可直接沿用）
2. 1b 的批次歷史價格函式要用小樣本+大樣本（數十檔 ticker）都驗證過效能可接受
3. 第2節殘餘風險：處理了幾項、跳過了哪幾項，如實在完工報告標注
4. 第3節舊檔清理：做了什麼判斷、依什麼判準，寫進 commit message
5. 完工報告清楚標注「真的完成」vs「待你拍板」的項目，不能寫成模糊的「目前進度」
